import { describe, it, expect } from 'vitest';
import { RadioGroup } from './RadioGroup';

describe('RadioGroup', () => {
  it('component exists', () => {
    expect(RadioGroup).toBeDefined();
  });
});
